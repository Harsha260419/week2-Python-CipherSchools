example = ('one', 'two', 'three')
# tuples are faster when compared to list and we use it when we know that we don't have to change the data anymore 
# no append, no insert, no pop, no remove

# methods that can be used in tuples
# count, index
# len function
# slicing
print(example[:2])